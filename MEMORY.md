# qva — Memória

Este ficheiro guarda aprendizagens persistentes para futuras sessões de agente neste repositório (`d:\ott\qva` — Question Vision Assistant).

## Regras Ativas

- `AGENTS.md` e `MEMORY.md` OFICIAIS deste projeto são os que estão na raiz do repositório (`d:\ott\qva\`). Qualquer sistema de auto-memória externo ao repo NÃO é fonte de verdade aqui — só estes dois ficheiros, versionados com o código.
- A fonte de verdade da especificação é `PROMPT DE DESENVOLVIMENTO — QUESTION VISION ASSISTANT.md` na raiz. `AGENTS.md` só resume e aponta para lá.
- Ler `AGENTS.md` e este ficheiro no início de cada sessão.
- Este projeto é independente de outros repositórios do utilizador (`d:\ott\odc`, `d:\ott\back`, `d:\ott\foa`) — não misturar memória/contexto entre eles.
- Não guardar segredos, tokens, passwords, connection strings completas ou dados pessoais desnecessários.
- **NUNCA ler ficheiros `.env` (nem qualquer ficheiro conhecido por conter segredos) com `Read`, `cat`, `type` ou qualquer comando que imprima o conteúdo completo.** Para verificar se uma variável existe, usar `grep -c "^VAR=" .env`.

## Preferências do Utilizador

- As interações neste chat devem ser em português.
- Ao final de cada interação/entrega, descrever explicitamente: Objetivo, Resultados e Próximo passo.
- Todas as etapas/tasks devem ser explicadas de forma didática — o que foi construído, por que essa opção foi escolhida, e que conceito está a ser demonstrado. Não entregar apenas um resumo técnico do diff.

## Decisões Técnicas Persistentes

- 2026-08-28: **Confirmado pelo utilizador**: este repositório (`d:\ott\qva`) é o do **Question Vision Assistant**. Os ficheiros `AGENTS.md`/`MEMORY.md` que aqui estavam pertenciam ao projeto `odc` (Ottimizia Driver Intelligence) e tinham sido copiados por engano — foram substituídos por versões próprias do QVA nesta data. O prompt de desenvolvimento na raiz é a fonte de verdade.
- Ordem de implementação (prompt secção 32): TASK-001 (estrutura + config) → TASK-002 (USBCamera) → TASK-003 (preview) → TASK-004 (captura manual) → TASK-005 (ImageProcessor) → TASK-006 (HttpLLMClient) → TASK-007 (QuestionExtractor) → TASK-008 (QuestionSolver) → TASK-009 (QuestionPipeline) → TASK-010 (WebSocket) → TASK-011 (UI mobile) → TASK-012 (FileCamera para testes) → TASK-013 (métricas de latência) → TASK-014 (testes) → TASK-015 (ChangeDetector) → TASK-016 (captura automática opcional) → TASK-017 (RTSPCamera).
- 2026-08-28: **TASK-001 concluída** — estrutura completa (`app/` com `api/`, `camera/`, `vision/`, `llm/`, `models/`, `services/`, `static/`), `config.py` (Pydantic Settings, tudo via `.env`, nenhum outro módulo lê `os.environ`), `logging_config.py` (JSON estruturado), `Dockerfile`/`docker-compose.yml`/`.env.example`/`requirements.txt`/`pytest.ini`. Módulos das tasks seguintes existem como esqueletos com interface pública definida + `NotImplementedError`. App arranca (`python -m app` / `uvicorn app.main:app`), 11 testes passam (`pytest`). Endpoints base: `/health`, `/api/llm/status`, `/api/camera/status`, `/api/capture` (501), `/ws` (stub).
- **Nota de ambiente**: host tem Python 3.10 (prompt pede 3.12+; Docker usa 3.12). Testes correm no host à mesma. `Settings(_env_file=None, ...)` nos testes para não ler o `.env` real.
- 2026-08-28: **TASK-002 concluída** — `USBCamera` (OpenCV `VideoCapture`) devolve frame BGR em memória, `_WARMUP_FRAMES=3` descartadas ao abrir, `_parse_device` aceita índice (`"0"`→0) ou caminho (`/dev/video1`). `is_available()` é uma sonda barata (abre→lê→fecha se não estava aberta) usada por `/health` e `/api/camera/status`, ambos agora reais e via `asyncio.to_thread` (I/O de hardware nunca no event loop). Nova `app/dependencies.py::get_camera` (singleton `@lru_cache` — webcam é recurso exclusivo). Testes com backend `cv2.VideoCapture` falso via `monkeypatch` + `FakeCamera` em `conftest.py` com `dependency_overrides` (sem exigir webcam). 18 testes passam.
- 2026-08-28: **TASK-003 concluída** — preview. `app/vision/encoding.py::encode_jpeg` (numpy→JPEG bytes em RAM, nunca `cv2.imwrite`; normaliza `cv2.error` de frame vazio para `ValueError`). `GET /api/camera/frame` (JPEG único, 503 se câmera indisponível) e `GET /api/camera/stream` (MJPEG `multipart/x-mixed-replace`, boundary `qvaframe`, ~10 fps). **Gerador `mjpeg_frames(camera, max_frames)` extraído da rota** — infinito em produção, limitado nos testes (o `TestClient` do Starlette consome o corpo todo antes de devolver, logo um stream infinito bloqueia — testar o gerador diretamente, nunca `client.get` sobre o `/stream`). UI (`static/`) mostra snapshot + checkbox "ao vivo". 24 testes.
- 2026-08-28: **TASK-004 concluída** — captura manual assíncrona. `POST /api/capture` → 202 com `capture_id` (uuid4), processamento via `BackgroundTasks`. `app/services/captures.py::CaptureRegistry` guarda o estado em memória (nunca imagens); `CaptureState` = os 7 estados visuais da secção 17 do prompt. `QuestionPipeline.process_capture(capture_id)` já orquestra os 5 passos (captura→imagem→extração→resolução→broadcast) com instrumentação `time.perf_counter()` por passo e emissão de eventos. **O pipeline nunca rebenta** — apanha tudo (`except Exception`) e devolve `ConsolidatedResponse(status="error", error=...)`; como os módulos de visão/LLM ainda são esqueletos, o fluxo termina em `error: not_implemented:...`, o que é esperado até TASK-005/006/007/008. `GET /api/capture/{id}` consulta o estado. Dependências ligadas em `app/dependencies.py` (`get_pipeline` compõe tudo via `Depends`). `conftest.py` limpa os singletons `@lru_cache` entre testes (fixture `autouse`). 29 testes.
- **Nota**: `BackgroundTasks` do `TestClient` corre síncrono após a resposta — por isso `GET /api/capture/{id}` logo a seguir a `POST` já vê o resultado nos testes.
- 2026-08-28: **TASK-005 concluída** — `ImageProcessor` (OpenCV). Fluxo: valida nitidez (variância do Laplaciano, `MIN_SHARPNESS=60`) e brilho (`40`-`248`; limite superior alto de propósito — documento/tela iluminado tem fundo perto do branco) → falha cedo com `ImageQualityError(reason)` (`blur_detected`/`too_dark`/`too_bright`/`empty_frame`) → `find_screen_quad` (Canny+dilate+`approxPolyDP`, maior contorno convexo de 4 lados entre 15% e 98% da área, rejeita cantos colados à borda = moldura do frame) → `warp_to_rect` (correção de perspetiva) → `deskew` (Hough, só ângulos <10°) → `enhance_contrast` (CLAHE no canal L de LAB) → `normalize_size` (max 1600px, nunca amplia). `ProcessedImage` com `sharpness_score`/`brightness_score`/`perspective_score` + `screen_detected`. Funções puras exportadas para teste isolado. `FakeCamera` do conftest agora devolve frame nítido 640x480 (passa as verificações de qualidade). 42 testes.
- **Achado**: `find_screen_quad` sobre ruído aleatório detetava a moldura do frame inteiro como "tela" (Canny fecha a borda da imagem) — resolvido rejeitando quadriláteros >98% da área ou com todos os cantos a ≤3px da borda.
- 2026-08-28: **TASK-006 concluída** — `HttpLLMClient` (`httpx.AsyncClient`). Assume formato *chat completions* estilo OpenAI (o mais comum em servidores locais: Ollama `/v1`, LM Studio, llama.cpp, vLLM) mas **nada é hardcoded** — URL/endpoint/modelo/key vêm de `Settings`. Payload multimodal (partes texto + `image_url` data URI) só quando `llm_supports_vision` e há `image_b64`. Tratamento de erro → `LLMError(kind)`: `timeout` (`httpx.TimeoutException`), `connection` (`ConnectError`/outros `HTTPError`), `http_4xx`, `http_5xx`, `bad_json` (corpo não-JSON), `invalid_response` (estrutura inesperada / conteúdo vazio). `health()` = GET barato ao host base (não gasta inferência). Cliente injetável nos testes via `httpx.MockTransport`. Ligado a `get_llm_client` (`@lru_cache`), `get_solver` recebe-o, `/health` e `/api/llm/status` reportam `reachable` real (`asyncio.gather` de camera+llm no `/health`). `lifespan` do FastAPI fecha o `AsyncClient` no shutdown. `conftest` tem `FakeLLM` + override. 53 testes.
- 2026-08-28: **TASK-007 concluída** — `QuestionExtractor`. `app/llm/prompts.py` (prompts de extração/resolução das secções 11/12, num só sítio). `app/llm/json_utils.py::extract_json_object` — parsing tolerante (bloco ```json, texto à volta, do 1º `{` ao último `}`) porque modelos locais nem sempre respeitam "sem markdown". Modo A (multimodal): `processed.image` → JPEG b64 (via `asyncio.to_thread`) → `LLMRequest` com `image_b64`. Modo B (OCR+LLM): `app/vision/ocr.py::OCREngine` (abstrato) + `TesseractOCR` (import tardio de `pytesseract`, `OCRUnavailableError` explícito se ausente — dependência opcional, comentada no `requirements.txt`) → texto no prompt, sem imagem. A escolha A/B é por `settings.llm_supports_vision`, invisível para o pipeline. `_parse` normaliza: tipo desconhecido → `UNKNOWN`, `options` não-dict → `{}`, `confidence` clamp [0,1], `code`/`formulas` vazios → `None`. `QuestionExtractionError` se a resposta não tiver JSON. Dependência `get_question_extractor` recebe `llm`+`settings`. 63 testes.
- 2026-08-28: **TASK-008 concluída** — `QuestionSolver`. Recebe `Question`, monta prompt de resolução (`SOLVE_SYSTEM` + `build_solve_user(question.model_dump_json())`), chama `LLMClient`, `_parse` normaliza (`answer` strip; `answer_text` preenchido de `question.options[answer]` se o LLM só devolveu a letra; `confidence` clamp; `ambiguous` bool). `LLMError` → `QuestionSolveError`; resposta não-JSON → `QuestionSolveError`. `clamp01` movido para `json_utils.py` (era duplicado em extractor+solver). Pipeline `_error_reason` reconhece `question_extraction_error`/`question_solve_error`. **O pipeline agora corre ponta a ponta e COMPLETA** com FakeCamera+FakeLLM (antes parava em `not_implemented`) — `test_capture_runs_pipeline_end_to_end` confirma questão+resultado+timing. 70 testes.
- 2026-08-28: **TASK-009 concluída** — `QuestionPipeline` consolidado. `ImageProcessor.process_sync` (público) corre agora em `asyncio.to_thread` no pipeline — antes `process()` async corria OpenCV no event loop. Context manager `_step(timing, field)` mede cada passo sem repetir `time.perf_counter()`. Logs estruturados nos 8 pontos da secção 26 (`CAPTURE_STARTED`/`CAPTURE_COMPLETED`/`IMAGE_PROCESSED`/`QUESTION_EXTRACTED`/`LLM_REQUEST_STARTED`/`LLM_REQUEST_COMPLETED`/`ANSWER_READY`/`PIPELINE_ERROR`) com `capture_id`+`request_id`. `ConsolidatedResponse` da secção 14 (id/status/question/result/timing). Pipeline nunca propaga exceção. Eventos WebSocket emitidos por transição. 6 testes novos focados no pipeline (ordem dos passos, estados, eventos em ordem, erro por passo para/reporta, funciona sem WebSocket). 76 testes.
- 2026-08-28: **TASK-010 concluída** — `WebSocketManager` real: `set` de ligações + `asyncio.Lock`, `connect` (accept+add), `disconnect` (discard), `broadcast` envia `{"event":..., "data":...}` a todas e **remove ligações mortas** (send falha → prune) para nunca bloquear o pipeline. Endpoint `WS /ws` (`app/api/websocket.py`): `manager.connect` → loop `receive_text` só para detetar `WebSocketDisconnect` → `disconnect` no `finally`. Testado ponta a ponta: `client.websocket_connect("/ws")` + `POST /api/capture` → o cliente recebe `capture_started`...`answer_ready` (secção 15, sem polling). UI liga ao `/ws` com reconexão automática; o botão Capturar já não faz polling. 82 testes.
- 2026-08-28: **TASK-011 concluída** — interface mobile. `index.html`/`styles.css`/`app.js` reescritos: layout mobile-first (`max-width:34rem`), tema escuro por omissão + `prefers-color-scheme: light`, header sticky com semáforo (câmera/LLM/servidor, `/health` a cada 15s). Cartões: preview (snapshot + toggle "ao vivo"), banner de estado, questão (tipo + enunciado + opções), resposta (letra+texto, confiança %, aviso de ambiguidade, explicação, timing). Os 7 estados da secção 17 mapeados em `STATE_LABEL` + `setState()` (botão desativa durante `capturing/processing_image/extracting_question/solving`). WebSocket para eventos + **polling de fallback** (`pollUntilDone`, 700ms) para os estados intermédios rápidos que um evento perdido não cobriria. `manifest.webmanifest` (PWA básica, sem ícones). App validada a arrancar de verdade (`uvicorn`, curl: index/js/health 200; câmera `true` neste host, LLM `false` sem serviço local). 85 testes.
- **Nota**: `app/static/` é servido pelo backend (`StaticFiles`), NÃO é um Artifact — a skill `artifact-design` não se aplica; segui a maquete da secção 16 do prompt.
- 2026-08-28: **TASK-012 concluída** — `FileCamera`. Carrega a imagem uma vez em `open()` (via `np.fromfile`+`cv2.imdecode`, não `cv2.imread` — este falha com caminhos não-ASCII no Windows), `capture()` devolve **cópia** independente. `CameraError` se ficheiro ausente/inválido. `tests/fixtures/make_fixture.py` gera `question.jpg` (questão MC sintética, nítida — passa o `ImageProcessor`); a fixture é versionada (`.gitignore` tem `!tests/fixtures/**`). **Teste end-to-end da secção 27/28**: `FileCamera` real + `ImageProcessor` real + `QuestionExtractor`/`Solver` com `FakeLLM` → `ConsolidatedResponse` completa, `error is None` (a imagem sintética não cai em `image_quality_error`). 92 testes.
- 2026-08-28: **TASK-013 concluída** — métricas de latência agregadas. `app/services/metrics.py::MetricsCollector` — contadores (total/completed/errors) + janela deslizante `deque(maxlen=200)` por passo, `snapshot()` devolve avg/p50/p95 por passo + success_rate. Só números, nunca conteúdo. Pipeline recebe `metrics` opcional e chama `record(timing, ok=...)` no fim (sucesso e erro). `GET /api/metrics` expõe o snapshot. `_percentile` por interpolação linear. 99 testes.
- 2026-08-28: **TASK-014 concluída** — cobertura de testes ~95% (`pytest-cov` adicionado). `HistoryStore` implementado de verdade (era `NotImplementedError`): deque(100) em memória, `save` só grava `completed` e **nunca imagens** (só id/created_at/question/result/timing), `get`/`delete`. Endpoints `GET/GET{id}/DELETE /api/history` (secção 19). Ligado ao pipeline (`get_history`, `store_history` da config). Testes novos: history (7), edge cases (6 — `encoding` wrap de `cv2.error`, `json_utils` vazio + `clamp01` não-numérico, `TesseractOCR` sem `pytesseract`, `USBCamera.close`/context manager). 111 testes.
- **Bugs encontrados**: (1) `datetime.UTC` é 3.11+ (host tem 3.10) → `timezone.utc`; (2) `@router.delete(..., status_code=204)` com `-> None` → FastAPI recusa body em 204, usar `Response(status_code=204)`; (3) nome da função de rota `get_history` colidia com a dependência importada `get_history` no `Depends()` → renomear rotas para `*_entry`.
- 2026-08-28: **TASK-015 concluída** — `ChangeDetector`. Score de diferença em [0,1] = `max(dHash bit-diff, 1 - correlação de histogramas de cinza)`. `register(frame)` define referência; `difference(frame)` → score (1.0 se sem referência); `is_new_question(frame)` → score ≥ `threshold` (config `CHANGE_THRESHOLD`, default 0.25). Ainda **não ligado** ao pipeline — a captura automática é TASK-016. `get_change_detector` em dependencies. 7 testes (mesmo frame < 0.05, ruído leve abaixo do limiar, cena diferente acima, register atualiza referência, aceita grayscale). Fixture de teste teve de gerar cenas mais densas (25 blocos) para serem visualmente distintas — dHash+histograma não distinguem 2 cenas com o mesmo layout.
- 2026-08-28: **TASK-016 concluída** — captura automática opcional. `app/services/auto_capture.py::AutoCaptureLoop` — `asyncio.Task` que a cada `_POLL_INTERVAL_S` (0.4s) lê frame → `ChangeDetector.is_new_question` → `_wait_stable` (espera `STABILIZATION_MS` sem mais mudança, via um `ChangeDetector` probe temporário sobre pares de frames consecutivos) → `pipeline.process_capture(job.id, frame=stable)` → `detector.register(stable)` (evita re-disparar). O loop nunca morre por erro (`except Exception` + log). `process_capture` ganhou `frame=None` opcional — captura automática passa o frame estável já observado, não recaptura. `build_pipeline()` (sem `Depends`) em dependencies para uso no lifespan. Lifespan do `main.py` arranca/pára o loop só se `AUTO_CAPTURE_ENABLED`. **Desligado por omissão** (MVP). 4 testes (loop desligado não arranca, dispara em mudança estável, não dispara em cena estática, stop idempotente).
- 2026-08-28: **TASK-017 concluída — MVP COMPLETO (17/17 tasks, 131 testes)**. `app/camera/rtsp.py::RTSPCamera` (+ `HTTPIPCamera` subclasse) via `cv2.VideoCapture(url, cv2.CAP_FFMPEG)`, `CAP_PROP_BUFFERSIZE=1`, descarta `_FLUSH_FRAMES=4` por captura (buffer do stream → frame fresco), `capture()` → `CameraError` se ligação perdida. `_redact()` esconde credenciais do URL nos logs. `factory.py` liga `CameraType.RTSP`/`HTTP`. **Critério de aceite Nº 13 validado**: `test_pipeline_unchanged_with_rtsp_camera` corre o pipeline completo com `RTSPCamera` (stream mockado) — `status=completed`, zero alterações ao pipeline/auto-captura/UI. É a 4ª implementação de `CameraSource` (USB/File/RTSP/HTTP), todas intercambiáveis.
- **Estado do projeto**: MVP funcional, 133 testes. Validação parcial com hardware real feita nesta máquina — webcam existe mas dá imagem muito escura/tapada (não dá para ler questões daqui); serviço LLM local não está a correr.
- 2026-08-28: **Fix pós-validação real (`ce73acd`)** — o backend MSMF por omissão do OpenCV no Windows abre a webcam mas falha silenciosamente em `read()`. `USBCamera` agora: `_open_any_backend` tenta `CAP_DSHOW`→`CAP_MSMF`→`CAP_ANY` e só aceita um backend que **consiga ler um frame** (não basta `isOpened()`); `capture()` tenta 5x e **rejeita frame preto** (brilho médio < 2) como `CameraError`. Com DirectShow a webcam deste PC passou a devolver frame real. Testes da USBCamera reescritos para a assinatura `(device, backend)`.
- 2026-08-29: **Validação real com Ollama + otimização de latência (140 testes)**. O utilizador tem Ollama em Docker (`ott-ollama`, porta 11434, só modelos de texto). Opção B escolhida: 2º Ollama isolado `qva-ollama-vision` (porta 11435, `docker-compose.ollama-vision.yml`, volume próprio) com `minicpm-v` + `llava:7b`. **Máquina sem GPU.**
  - **Descoberta importante — a API OpenAI-compat do Ollama funciona com imagens** (`image_url` data URI), o `HttpLLMClient` não precisou de mudanças. O 1º teste que falhou foi por 2 modelos de visão a servir em CPU em paralelo (recursos esgotados), não por formato.
  - **Latências reais medidas** (fixture, modelo quente): modo A `minicpm-v` ~2min; modo B `qwen2.5:7b` 2 chamadas ~48s; modo B fundido `qwen2.5:7b` ~22s. Meta <5s da spec só com GPU/cloud.
  - **OCR sem binário**: `rapidocr-onnxruntime` (ONNX) adicionado ao `requirements.txt` — o binário `tesseract` não está instalado e não há winget/choco na máquina. `app/vision/ocr.py::build_ocr_engine()` tenta RapidOCR → Tesseract. `RapidOCREngine` transcreveu a fixture perfeitamente (~1s modelo quente).
  - **Fusão do modo B**: `QuestionExtractor.extract_and_solve()` + `supports_combined` (True só no modo B). Prompt `COMBINED_SYSTEM` (JSON PLANO — `phi3.5` corrompia JSON aninhado; `qwen2.5:7b` faz JSON plano bem). Pipeline: se `extractor.supports_combined`, salta o `QuestionSolver`. `phi3.5:3.8b` testado (2× mais rápido) mas **não fiável para JSON** — descartado, `qwen2.5:7b` é a escolha.
  - **Bug de teste corrigido**: o `client` fixture lia o `.env` real da máquina (`get_settings()` sem `_env_file=None`) → ativava modo B + OCR real nos testes. `conftest._isolated_settings` (autouse) força `Settings` de teste em `app.config`/`app.dependencies`/`app.main` + `dependency_overrides` no client.
  - **Fix câmera Windows** (`ce73acd`, sessão anterior): `USBCamera` tenta `CAP_DSHOW`→`CAP_MSMF`→`CAP_ANY`, rejeita frame preto.
- **Próximos passos**: 1) **validar com FOTO REAL de um ecrã** (webcam desta máquina dá imagem preta — usar `CAMERA_TYPE=file` com screenshots do utilizador, ou webcam noutra máquina); 2) calibrar limiares (`MIN_SHARPNESS=60`, brilho, `CHANGE_THRESHOLD=0.25`); 3) auth por token (`AUTH_TOKEN` existe, nenhum endpoint verifica); 4) UI consumir `/api/history`; 5) merge `dev`→`main`.
- **Decisão em aberto para o utilizador**: aceitar ~20s/questão em CPU, OU usar API cloud (Groq faz visão <2s; muda `LLM_BASE_URL`+`LLM_API_KEY`, deixa de ser local-first), OU GPU noutra máquina.

- 2026-08-29: **Primeira captura ao vivo bem-sucedida** — utilizador apontou webcam ao ecrã, "Qual estrutura segue LIFO?" → "B) Pilha, 90%" correto, 44s. OCR trocou letras (Flo/Piiha/Listo) por qualidade de imagem (reflexo, foco, metade do frame era parede) — resposta certa à mesma por contexto.
- 2026-08-29: **Calibração do `ImageProcessor` para fotos de webcam→ecrã** — `MIN_SHARPNESS` 60→25; `find_screen_quad` área mínima 15%→8%, `approxPolyDP` com mais folga (0.02→0.04), aceita 5-8 vértices via `minAreaRect` (perspetiva ligeira raramente dá 4 cantos exatos), Canny/dilate mais permissivos. **Nota**: a webcam integrada do notebook aponta para o utilizador, não para o próprio ecrã — "ler o ecrã do notebook com a webcam do notebook" é geometricamente impossível; precisa de webcam externa ou apontar a outro ecrã.
- 2026-08-29: **Escolha de câmera na UI implementada** — `app/camera/manager.py::CameraManager` (`CameraSource` que delega numa câmera ativa trocável, fecha a antiga ao trocar, valida `is_available()` antes de assumir). `get_camera_manager` (`@lru_cache`) substitui `get_camera` como singleton; `get_camera` agora devolve o manager. Endpoints `GET /api/camera/devices` (sonda USB 0-4) e `POST /api/camera/select` ({kind: usb|file|rtsp|http, target}). UI: seletor no cartão de preview (USB / Wi-Fi RTSP / Wi-Fi HTTP). `conftest` tem `FakeCameraManager`. 147 testes.
- 2026-08-29: **Deteção de câmeras corrigida** — a máquina do utilizador tem 2 câmeras: idx 0 = Logi C270 USB (só responde por DSHOW), idx 1 = HP 5MP interna (só por MSMF). Sondar por `cv2.VideoCapture` demorava ~17s (timeout interno do MSMF em índices inexistentes) e reportava backend inconsistente sob paralelismo. **Solução: `list_video_input_names()` via `pygrabber` (DirectShow)** — enumera por nome, instantâneo (0.14s), no Windows; `list_devices()` usa isto e cai na sonda por índice só se `pygrabber` faltar. `_BACKENDS` reordenado para `MSMF→DSHOW→ANY`. `pygrabber==0.2 ; sys_platform=="win32"` no `requirements.txt`. 148 testes.
- 2026-08-29: **Câmera do browser implementada (`CAMERA_TYPE=browser`)** — a câmera do dispositivo que abre a UI (telemóvel). `app/camera/browser.py::BrowserCamera` guarda o último JPEG enviado (TTL 15s, thread-safe); `POST /api/camera/upload-frame` (corpo = JPEG binário). `CameraManager` mantém uma `BrowserCamera` única (tem estado), `select("browser")` aponta para ela sem recriar. UI (`app.js`): `getUserMedia` + `<video>` + captura de frame via `<canvas>.toBlob`; ao Capturar no modo browser, envia o frame primeiro. Botão "alternar câmera" (frente/trás via `facingMode`). Se o servidor não tiver câmeras, a UI arranca já no modo browser. **Validado ponta a ponta**: upload frame → capture → "B) Pilha" correto, 24s, `capture_ms=0.8` (não abriu hardware). 158 testes.
- 2026-08-29: **HTTPS implementado** (`HTTPS=true`) — `app/tls.py::ensure_cert()` gera cert/chave self-signed em `certs/` (via `cryptography`, já disponível) com SAN = `localhost` + todos os IPv4 da máquina (`socket.getaddrinfo` + rota de saída para `8.8.8.8`). `app/__main__.py` passa `ssl_certfile`/`ssl_keyfile` ao `uvicorn.run` e imprime os URLs `https://<ip>:porta/` no arranque. `cryptography==45.0.3` no `requirements.txt`; `certs/` no `.gitignore`. **Validado**: `https://127.0.0.1:8443/health` OK, HTTP na mesma porta rejeitado. 162 testes. Agora o telemóvel pode usar `CAMERA_TYPE=browser` via `https://<ip-lan>:8080/` (aceitar o aviso de certificado uma vez).
- 2026-08-29: **Câmera USB "sumia" intermitentemente — CAUSA RAIZ encontrada e corrigida**. A `pygrabber` sempre viu as 2 câmeras; o problema era **concorrência**: `/health` (polling 15s do `app.js`) + `/api/camera/status` + `/api/camera/frame` (preview) chamavam `is_available()`/`capture()` em paralelo sobre a **mesma `VideoCapture`** (não thread-safe), e o `is_available()` da `USBCamera` **abria→lia→fechava** a webcam a cada chamada. A Logi C270 é lenta a re-inicializar (~1-2s) → pedidos cruzados davam `CameraError` → UI marcava a câmera como sumida. **Correção** (`CameraManager`): (1) `threading.RLock` serializa TODO o acesso (open/capture/close/is_available/select); (2) `is_available()` do manager já não delega no da câmera (que fecha) — abre+lê diretamente e mantém aberta; (3) cache de disponibilidade `_AVAILABILITY_CACHE_S=8s` — `/health` repetido não volta a sondar a webcam; (4) `capture()` marca disponível sem sondar. Lifespan fecha a câmera no shutdown. Validado com 18 pedidos concorrentes → todos 200. 171 testes.
- **Nota anterior (ainda válida)**: um `python -m app` órfão de sessões de teste (porta 8080) também segura a câmera — `taskkill` deste shell não mata o processo do PowerShell do utilizador; `Ctrl+C` + `Get-Process python`.
- 2026-08-29: **Otimização de latência** — (1) `LLM_MODEL=llama3.2:3b` (novo no Ollama do utilizador) — ~10s vs. ~25s do `qwen2.5:7b`, JSON fiável, **mas erra cálculo/lógica** (respondeu "7*8 = C) Pilha"). `qwen2.5:7b` continua a alternativa fiável. `.env.example` documenta o trade-off. (2) `get_ocr_engine()` (`@lru_cache`) — motor RapidOCR único, **pré-aquecido no `lifespan` via `_warm_ocr`** — a 1ª captura já não paga o carregamento dos modelos ONNX (~5s→0). `get_question_extractor` e `build_pipeline` passam-no ao `QuestionExtractor`. **Resultado: ~14-16s/questão** (era ~22-45s). 162 testes.
- 2026-08-29: **DECISÃO: modo de leitura = API cloud de visão**. Depois de testes exaustivos em CPU: OCR (RapidOCR + qwen2.5:7b, ~25s) funciona SÓ com 1 questão limpa enquadrada — com folhas de várias questões junta fragmentos e o LLM inventa uma questão (visto na prática: imagem `ChatGPT Image...` com diagrama de arquitetura + fragmentos de questões → respondeu "fila O(1)" inexistente). Modelos de visão locais (`qwen2.5vl:3b`, `minicpm-v`) leem o layout corretamente MAS numa imagem grande (folha 1536×1024) demoram >3-5min ou travam em CPU. `moondream` não lê texto. **Utilizador escolheu API cloud** (OpenRouter/Gemini, ~3s, ~$0.001/imagem, imagem só enviada na captura). `.env.example` reescrito com exemplos OpenRouter/Gemini/local. Default `.env`: `LLM_MODE=vision`, `LLM_BASE_URL=https://openrouter.ai/api`, `LLM_MODEL=google/gemini-2.0-flash-exp:free`, falta só `LLM_API_KEY`.
- 2026-08-29: **API cloud de visão VALIDADA e a funcionar** — `LLM_MODE=vision` + OpenRouter + `google/gemini-3.7-flash`: testado com a folha real de 20 questões (`ChatGPT Image...`, 1536×1024) → `STATUS: completed` em **12.6s**, leu uma questão real da folha com as 4 opções corretas e resolveu certo. Contraste com OCR que inventava opções incompletas. **Modelos `:free` da OpenRouter têm rate limit inutilizável (429)** — precisa de créditos (~$5, ~$0.0002/questão com gemini-3.7-flash). Alternativas testadas OK: `z-ai/glm-5.3-flash`, `qwen/qwen3.8-27b`. `health()` do `HttpLLMClient` agora aceita qualquer resposta HTTP <500 (incl. 401/404) como "acessível" — antes GET ao base da OpenRouter dava 404 → LLM vermelho. `list_models()` tenta `/api/tags` E `/v1/models`. `/api/llm/status` devolve `mode` e `cloud`; UI mostra badge "VISION · cloud". **NOTA: a data do sistema é 2026, por isso os nomes de modelos parecem futuristas (gemini-3.7, qwen3.8) — são os reais nesta timeline.**
- **Arquitetura de estratégia implementada**: `LLM_MODE` = `ocr`|`vision`|`hybrid`. `QuestionExtractor` com `_mode`, `_vision_llm` (via `get_vision_llm_client` — `LLM_VISION_BASE_URL`/`MODEL` separados, ou reutiliza o principal se já for visão). `hybrid` tenta OCR e cai para visão se os guards de alucinação falharem. Guards: `_guard_ocr_text` (mín. 25 chars), `_guard_question_matches_ocr` (overlap de palavras significativas ≥35% E bigramas ≥20% — stopwords filtradas), `_guard_question_not_empty` (visão). 172 testes.
- 2026-08-29: **Seletor de modelo na UI implementado** — `HttpLLMClient` ganhou `model` (property), `set_model()`, `list_models()` (via `/api/tags` do Ollama). Endpoints `GET /api/llm/models` (interseção do que o serviço expõe com `LLM_MODELS_ALLOWED`; inclui sempre o ativo) e `POST /api/llm/select`. Config `LLM_MODELS_ALLOWED` (CSV, vazio = todos). UI: `<select>` no cabeçalho, troca em runtime; como o `HttpLLMClient` é singleton, extractor/solver usam o novo modelo sem reconstruir. `.env`: `LLM_MODELS_ALLOWED=qwen2.5:7b-instruct,llama3.2:3b`. **Recomendação de modelos** (testados em CPU): `qwen2.5:7b-instruct` (fiável, ~25s, padrão) e `llama3.2:3b` (~10s, bom p/ definições, ERRA cálculo/lógica). Descartados: `phi3.5`/`llama3.2:1b` (corrompem JSON), `mistral:7b` (pior em PT), `llama3.1:8b` (lento sem ganho). 169 testes.
- **Ainda por fazer**: validar no telemóvel real (HTTPS + câmera do browser); validar calibração + escolha de modelo com fotos reais; auth por token; UI histórico; merge `dev`→`main`.
