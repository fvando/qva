# Question Vision Assistant — dependências do backend (TASK-001)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
opencv-python-headless==4.10.0.84
numpy==2.2.1
httpx==0.28.1

# OCR para o modo B (LLM_SUPPORTS_VISION=false).
# RapidOCR: ONNX, CPU, sem binário externo — preferido.
rapidocr-onnxruntime==1.4.4
# Alternativa clássica (requer o binário `tesseract` no sistema):
# pytesseract==0.3.13

# Dev / testes
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
